name: testrailway
builds:
  - name: static
    type: static
    source: .
services:
  - name: web
    static:
      directory: .
    ports:
      - port: 80
        protocol: HTTP
    routes:
      - path: /
        port: 80
